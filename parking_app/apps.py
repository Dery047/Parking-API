from django.apps import AppConfig


class ParqueoConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'parking_app' #name of the app, must be the same as the folder name.
